# Workly 로컬 인프라 — Docker Desktop 설치 후 `docker compose up -d`
services:
  postgres:
    image: postgres:16
    container_name: workly-postgres
    environment:
      POSTGRES_USER: workly
      POSTGRES_PASSWORD: workly
      POSTGRES_DB: workly
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: workly-redis
    ports:
      - "6379:6379"

  meilisearch:
    image: getmeili/meilisearch:v1.10
    container_name: workly-meili
    environment:
      MEILI_MASTER_KEY: workly_dev_key
      MEILI_ENV: development
    ports:
      - "7700:7700"
    volumes:
      - meilidata:/meili_data

volumes:
  pgdata:
  meilidata:
